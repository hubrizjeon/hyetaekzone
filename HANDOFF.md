# 돌봄플러스 혜택존 — 시스템 인수인계 / 교차검토 문서

> 이 문서는 다른 AI(ChatGPT 등)나 외부 개발자가 이 프로젝트를 **처음부터 이해하고 함께 개발**할 수 있도록 작성된 자립형 문서입니다.
> 최종 갱신: 2026-09-10 (목)

---

## 1. 프로젝트 개요

**무엇인가**
돌봄·가사 종사자를 위한 "오늘의 실속 혜택" 단일 페이지. 정부 지원부터 마트 전단·카페 쿠폰까지, 그날 실제로 쓸 수 있는 혜택만 큰 글씨로 모아 보여준다.

**누가 쓰는가**
- 아이돌보미, 가사관리사, 외국인 가사관리사
- 20~60대 여성, **시니어 포함**
- 스마트폰으로 접근. 로그인 없음.

**설계 제약 (바꾸면 안 되는 것)**
- 본문 최소 18px, 카드 제목 23px, 버튼 20px — 시니어 가독성
- 흰 배경 (`--bg:#ffffff`), 고대비
- 카드 하단 **큰 버튼** = 공식 링크 (탭 영역 확보)
- 다크모드 대응 (`:root[data-theme="dark"]`)
- 외부 의존성 0 — 단일 HTML, JS는 날짜 표시 한 줄뿐

---

## 2. 현재 상태

| 항목 | 값 |
|---|---|
| 공개 URL | https://hubrizjeon.github.io/hyetaekzone/ |
| 저장소 | https://github.com/hubrizjeon/hyetaekzone (public) |
| 호스팅 | GitHub Pages, `main` 브랜치 / root |
| 로컬 경로 | `~/hyetaekzone` |
| HTTPS | 강제 적용 |
| 커스텀 도메인 | 미연결 (benefits.hubriz.io 검토 중) |

### 파일 구조
```
~/hyetaekzone/
├── index.html              # 페이지 본체 (단일 파일, 약 30KB)
├── hotdeal.html            # 🔥 오늘의 핫딜 — 쿠팡파트너스 상품 전용 (scripts/hotdeal.py 가 생성)
├── scripts/hotdeal.py      # 골드박스 → 핫딜 칸·hotdeal.html 생성 (매일 2회 자동 실행)
├── scripts/coupang.py      # 쿠팡파트너스 API 클라이언트 (키: ~/.hyetaekzone.env)
├── mockup/                 # 핫딜 시안 (noindex, 예시 상품)
├── update.sh               # 원클릭 배포: git add/commit/push
├── setup.sh                # 최초 1회 저장소 생성 + Pages 활성화
├── README.md
├── HANDOFF.md              # 이 문서
├── blog-assets/            # 블로그 메일용 샘플 이미지 (760×400 JPEG)
│   ├── hero.jpg  gov.jpg  health.jpg  family.jpg
│   └── shop.jpg  beauty.jpg  cafe.jpg
└── blog-drafts/{YYYYMMDD}/ # 일자별 블로그 워드파일
```

### 배포 방법
```bash
~/hyetaekzone/update.sh "커밋 메시지"
```
Pages 반영까지 30~90초. `git config`는 저장소 로컬에 고정됨 (`HUBRIZ` / `ceo@hubriz.io`).

---

## 3. 페이지 구조

### 섹션 순서
```
header (브랜드 · h1 · 날짜 · 인사말)
목차 (.toc — 7개 앵커 링크)
🆕 #new     오늘 새로 바뀐 혜택   ← 빨간 테두리, 강조
🏛️ #gov     1. 정부·우리 동네 혜택
💊 #health  2. 내 몸 챙기기
🎁 #family  3. 가족·경조사·추석
🛒 #shop    4. 장보기·생활
💄 #beauty  5. 뷰티·옷
☕ #cafe    6. 커피·먹거리·배달
💡 .tips    오늘의 절약 꿀팁 3
📎 .sources 출처
footer      갱신일 · 주의문구
```

### 카드 스키마 (모든 카드가 동일)
```html
<div class="card {g|h|f|s|b|c|nw}">
  <div class="tagrow">
    <span class="where">브랜드명</span>
    <span class="newtag">오늘 시작</span>      <!-- NEW 섹션 전용, 선택 -->
  </div>
  <div class="what">핵심 혜택 <span class="hi">강조 숫자</span></div>
  <div class="how"><span class="ic">👉</span><span>받는 법</span></div>
  <div class="how"><span class="ic">📅</span><span>부가 설명</span></div>
  <span class="badge {ok|warn|info}">2026.9.10 ~ 9.16</span>
  <a class="btn" href="공식링크" target="_blank" rel="noopener">버튼 문구 <span class="arr">→</span></a>
</div>
```

**카드 클래스 = 카테고리 색상**
`g`=정부(파랑 #2563eb) `h`=건강(초록 #16a34a) `f`=가족(핑크 #db2777)
`s`=장보기(주황 #d97706) `b`=뷰티(보라 #9333ea) `c`=카페(청록 #0891b2) `nw`=신규(빨강 #dc2626)

**뱃지 의미**
- `ok` (초록) = 기간 명확, 진행 중
- `warn` (빨강) = 마감 임박 또는 종료
- `info` (파랑) = 상시 혜택 또는 "앱에서 확인"

### 날짜 자동 표시
헤더 날짜는 접속일 기준으로 클라이언트에서 갱신된다.
```html
<div class="date" id="today">2026년 9월 10일 (목)</div>
...
<script>(function(){var d=new Date(),D=['일','월','화','수','목','금','토'];
var e=document.getElementById('today');
if(e)e.textContent=d.getFullYear()+'년 '+(d.getMonth()+1)+'월 '+d.getDate()+'일 ('+D[d.getDay()]+')';})();</script>
```
> ⚠️ 이 때문에 **콘텐츠가 낡아도 날짜만 최신으로 보이는** 위험이 있다.
> 그래서 푸터에 `콘텐츠 최종 갱신: <b>YYYY년 M월 D일</b>`를 **하드코딩**해 두어 낡음을 감지할 수 있게 했다.

---

## 4. 자동화 파이프라인

`~/.claude/scheduled-tasks/` 에 저장된 두 개의 스케줄 작업이 돌아간다.

### (A) 매일 23:04 — 야간 갱신 (새 혜택 조사)
`hyetaekzone-nightly-update` · 2026-09-11 지시문 교체 완료 (대표 승인)
1. `git pull` → 대상 날짜 결정 (18시 이후 실행이면 **내일자**) · 요일 2중 검증
2. 오늘자 `야간 갱신 — M/D(요일)자` 커밋이 이미 있으면 **건너뜀** (다른 세션과 중복 방지)
3. WebSearch로 조사 → NEW 섹션·혜택 카드·꿀팁·출처·푸터 날짜만 수정
4. **보존 블록(PARTNERS, HZ-META/TOOLS/SHARE/TRACK/JS, HZ-KIT-CSS, HOTDEAL-CSS)과 목차 「🔥 핫딜 상품」 칩은 절대 수정 금지** — 수정 전후 블록이 글자 하나까지 같은지 검사
5. `python3 scripts/expire.py --check --today {대상일}` 로 기간 지난 카드 없음 확인
6. `update.sh "야간 갱신 — M/D(요일)자"` → 200·인코딩 검증
7. 요약 메일은 **ceo@hubriz.io 만** (2026-09-10 대표 지시). 막히면 초안 + 알림

### (B) 매일 08:04 — 블로그 완성 원고 (새소식만)
`hyetaekzone-morning-blog-mail` · 2026-09-11 재활성화 · 허수빈 요청 기준: 공백 제외 2,000자↑ · 사진 4~5장 · 이전 원고와 겹치는 문장 0 (`scripts/blog_check.py` 통과해야 발송)
1. `git pull` → NEW 섹션 카드 중 **`blog/ledger.json` 에 없는 글감만** (대장 규칙: 같은 key 금지, 상시 30일, 제목 14일, 전날 사진 금지)
2. 없으면 한 줄 메일만
3. 원문 사실 확인 → Pexels 사진(직접 보고 고름) → `blog/{YYYYMMDD}.html` (복사 버튼) + `blog/{YYYYMMDD}_word.docx`
4. 수신 **허수빈(supeda00@) / 참조 ceo@** — 2026-09-11 대표 지시로 재개. 첨부 없이 **링크로만** (크롬 불필요)
5. 발송 성공 후에만 `ledger.json` 에 추가

> 스케줄 작업은 **Claude 앱이 켜져 있을 때만** 실행된다. 꺼져 있으면 다음 실행 시점으로 밀린다.

---

### (C) GitHub Actions — 자동 정리·핫딜 (2026-09-10~)
`.github/workflows/auto.yml` · 컴퓨터·Claude 앱이 꺼져 있어도 GitHub 서버에서 실행
- **00:05 KST** 기간 지난 혜택 카드 자동 삭제(`scripts/expire.py`, 날짜가 분명한 카드만) + 핫딜 새로고침
- **07:30 KST** 핫딜 새로고침
- 쿠팡 키는 GitHub 저장소 비밀값 `COUPANG_ACCESS_KEY`·`COUPANG_SECRET_KEY` (등록: `scripts/set-github-secrets.sh`, 대표가 직접). 없으면 핫딜 단계만 건너뜀
- 쿠팡 조회가 통째로 실패하면 기존 핫딜을 그대로 둠
- 새 혜택 조사·블로그는 AI 가 필요해 (A)(B) Claude 예약 작업이 담당
- 메일 수신: **당분간 ceo@hubriz.io 만** (대표 지시 2026-09-10 — 허수빈 제외)

---

## 5. 데이터 품질 규칙 (핵심)

이 페이지는 **저소득 취약계층이 포함된 독자**에게 정부 지원 정보를 안내한다. 잘못된 정보의 비용이 크다.

**반드시 지킬 것**
1. 모든 할인율·기간·조건은 **브랜드 공식 페이지 또는 언론 원문**으로 확인 후 기재
2. 확인 불가 → 넣지 않거나 `info` 뱃지로 "앱에서 최신 확인" 처리
3. 유효기간이 오늘 이전인 이벤트는 **반드시 제외**
4. 같은 브랜드 동일 혜택은 1카드로 통합 (중복 금지)
5. 과장 표현 금지 (근거 없는 "연중 최저가" 등)
6. `search.naver.com` 직접 스크랩, 인스타그램 스크랩 **금지**

**소스 우선순위**
브랜드 공식 이벤트 페이지 → 편의점 집계(pyony.com) → 카드고릴라 → 통신사 멤버십 → 언론 원문(뉴시스·연합·머니투데이·헤럴드 등)

---

## 6. 해결된 이슈 (재발 방지용 기록)

| 이슈 | 증상 | 원인 | 조치 |
|---|---|---|---|
| **요일 오류** | 9/9을 "(화)"로 표기 (실제 수요일) | zsh 배열은 **1부터** 시작하는데 `date +%w`(0=일) 값으로 0-base 인덱싱 | 프롬프트에 `date +%A` / `cal` 2중 검증 명시 |
| **커밋 실패** | 야간 작업이 돌았는데 사이트 갱신 안 됨 | `git config user.email` 미설정 → `unable to auto-detect email address` | 저장소 로컬 config에 `HUBRIZ`/`ceo@hubriz.io` 고정 |
| **한글 깨짐** | 배지 문구가 `ì§ë íì¬`로 출력 | `perl -CSD` 사용 시 `-e` 스크립트 리터럴이 디코딩되지 않아 이중 인코딩 | `-CSD` 제거, 바이트 시맨틱으로 치환 |
| **메일 발송 차단** | MCP `send_message` 권한 거부 | 호스트 권한 정책 | 초안 생성 → 브라우저로 검증 후 발송 |
| **첨부 실패** | index.html base64가 40,225자 | 인라인 전달 한계 | 링크 방식으로 대체, 파일은 별도 전달 |
| **이미지 깨짐 우려** | 외부 스톡 핫링크 불안정 | 외부 의존 | 샘플 이미지 7종 자체 생성 후 저장소 호스팅 |

---

## 7. 미해결 / 결정 필요

### (a) 🔥 오늘의 핫딜 (쿠팡파트너스) — **2026-09-10 구현·공개**

**현재 동작 (이 부분이 최신입니다. 아래 "설계 당시 기록"과 다르면 이쪽을 따릅니다)**
- 메인: 혜택 6개 카테고리 뒤 · 「오늘의 절약 꿀팁」 **앞**에 핫딜 칸 — 대표 3개(서로 다른 카테고리) + 「카테고리별로 보기」 버튼(최대 8개) + 「🔥 핫딜 상품 전체 보기」
- 목차 **두 번째 칸 「🔥 핫딜 상품」**(첫 칸 「🆕 새소식」 바로 옆) → **`hotdeal.html`** (쿠팡파트너스 상품만, 혜택 정보 섞지 않음). 두 칸의 문구·순서는 scripts/hotdeal.py 가 매번 바로잡음
- **매일 유지** — 칸과 칩은 없어지지 않음. 통과 상품 0개거나 API 실패 시 골드박스 바로가기 제휴 카드 1장
- 상품 출처: **쿠팡 골드박스(25개) + 카테고리 19개 × 인기 상품 20개** (중복 제거 후 약 340개)
- **가격 상한·카테고리 제한 없음** (2026-09-10 대표 결정 — 여행·가전·반려동물 등 전부). 이름·가격·링크·이미지가 빠진 상품만 뺌
- 분류는 카테고리 번호가 아니라 **상품마다 쿠팡이 준 `categoryName`** 으로 (번호와 내용이 다름: 1012·1024 → 신선식품, 1025·1026 → 여행 도서). 여행 이용권은 골드박스에 올라온 날만 있음
- 핫딜 페이지: 필터 「전체 · 🔥 골드박스 특가 · 카테고리별」, 정렬 「쿠팡 인기순 · 낮은 가격순 · 높은 가격순」, 처음 30개 + 「상품 더 보기」 30개씩
- **할인율·정가 표시 없음, 할인순 정렬 없음** — 골드박스·카테고리·검색 API 모두 판매가만 줌 (지어내면 거짓 표시). 가장 가까운 필터가 「골드박스 특가」
- 카드: 상품 사진, 상품명, 쿠팡 판매가 + 확인 시각, "쿠팡 골드박스 선정" 또는 "쿠팡 ○○ 인기 상품", 로켓배송 여부, 버튼 **「구매하러 가기」**
- 링크: 모두 쿠팡파트너스 제휴 링크(`lptag=AF7078394`, `subid=hyetaekzone`), `rel="noopener nofollow sponsored"`
- **네이버 언급·비교 링크 전부 삭제** (대표 결정). 담당자 주 1회 가격 직접 점검
- 새로고침: `python3 scripts/hotdeal.py` — 야간 갱신(23시)과 아침 작업(08시)이 매일 실행 (API 호출 20회/회).
  index.html 의 `PARTNERS` 마커 · `HOTDEAL-CSS` 마커 · 목차 칩만 건드림. 사람이 손으로 고치지 말 것
- 이 저장소에서 '갱신'이라는 말은 **혜택 갱신 커밋에만** 씁니다 (야간 작업 중복 방지 검사가 이 말로 판단)
- 토스 쉐어링크: 여전히 보류 (약관 확인 전)

**설계 당시 기록 (참고용 — 네이버 비교 규칙은 폐기)**

> 2026-09-10 결정: **API 키 확보 전까지 오픈하지 않는다.**
> 이유 — "네이버 쇼핑보다 비싼 물건은 노출하지 않는다"가 이 기능의 핵심 원칙인데,
> 검증 수단(API) 없이 여는 것은 그 원칙과 정면으로 어긋나기 때문.

**확정된 배치 (안1 + 안3 혼합)**
- 상단 목차 맨 끝에 빨간 테두리 칩 `🔥 오늘의 핫딜` 추가 → 클릭 시 하단 섹션으로 이동
- 섹션 위치는 혜택 6개 카테고리가 모두 끝난 뒤, **굵은 구분선 아래 / 번호 없음**
- 혜택 카테고리 안에는 광고를 **절대 넣지 않는다**
- 카드: 점선 테두리(#e5b9c4) + 회색 `광고` 배지 + 빨강(#e11d48) 버튼
- 보존 마커 `<!-- PARTNERS:START -->` / `<!-- PARTNERS:END -->` 로 감싸고,
  야간 자동 갱신 프롬프트에 "이 블록은 수정·삭제 금지" 명시

**고지 문구 (섹션 헤더 바로 아래, 본문과 같은 18px)**
```
이 영역은 제휴 링크입니다.
이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.
구매하셔도 여러분이 내시는 가격은 같습니다.
가격·할인율은 수시로 바뀌니 쿠팡에서 최종 가격을 확인해 주세요.
```
마지막 줄은 필수 — "핫딜"이라는 이름이 *실제로 싸다*는 주장을 담기 때문.

**상품 그룹 (30~60대 여성 기준, 시즌 반영)**
🎁 추석 준비 · 💊 건강 챙기기 · 🏠 주방·생활 소모품 · 🍂 환절기 · 👟 편한 신발·옷 · 💄 뷰티
→ 시즌에 따라 교체 (추석 후 김장, 겨울 난방·보온 등)

**가격 비교 규칙 (핵심)**
- 노출 조건: `쿠팡 실구매가(배송비 포함) ≤ 네이버 최저가(배송비 포함)`
- **fail-closed** — 아래는 전부 제외:
  - 쿠팡이 더 비쌈
  - 가격 조회 실패
  - 네이버에서 동일 상품 매칭 실패
- 매칭 시 제조사·모델명·**용량/개수까지 일치** 확인 (50포 vs 30포 오비교 방지)
- 통과 상품 0개 → **섹션 자체를 숨김**
- 각 카드에 "네이버 최저가 직접 비교해보기" 링크 병기 (가격 변동 대비 투명성 장치)
- 카드는 카테고리 링크가 아니라 **개별 상품(SKU) 단위**

**⚠️ 2026-09-10 확인: 네이버 쇼핑검색 API가 종료되었습니다**

네이버 개발자센터 API가 **NAVER API HUB(네이버클라우드)** 로 이관되면서 일부는 완전 종료됨.

| API | 상태 |
|---|---|
| 쇼핑검색 (상품명·**최저가**·URL) | ❌ **2026.7.31 완전 종료 · 대체 API 없음** |
| 책검색 / 책상세 / 전문자료 | ❌ 종료 |
| Shopping Insight (데이터랩) | ✅ HUB로 이관 — 단, **클릭 추이만** 제공 (가격·상품명 없음) |
| Search Trend, 뉴스·블로그·이미지 등 | ✅ HUB로 이관 |

- 신규 사용자는 developers.naver.com이 아니라 **네이버클라우드 플랫폼 → NAVER API HUB**에서 신청
- 기존 개발자센터 키는 2027.6.30까지 한시 유예 (2026.7.30까지 신청한 경우)
- 출처: 네이버 공지 32530 및 이관 가이드 분석

**→ "네이버 최저가와 비교" 규칙은 공식 API로 자동화 불가.** 크롤링은 약관 위반이라 채택하지 않음.
대안은 아래 셋의 조합:
1. 쿠팡 자체 할인 검증 — goldbox(당일특가) + originalPrice/salePrice 할인율
2. 카드마다 "네이버 최저가 직접 비교" 링크 (우리가 최저가를 **주장하지 않음**)
3. 담당자 주 1회 수동 스팟체크

**구현에 필요한 것 (미확보)**
| 항목 | 발급처 | 용도 | 상태 |
|---|---|---|---|
| 쿠팡파트너스 ACCESS/SECRET KEY | 파트너스 관리자 | 상품·가격·딥링크·리포트 | 확인됨 |
| NAVER API HUB 키 | 네이버클라우드 플랫폼 | **Shopping Insight**(인기·시즌) 전용 | 미발급 |

쿠팡파트너스 가입 시 사이트 URL `https://hubrizjeon.github.io/hyetaekzone/` 등록 필요.
카드별 `subId`를 다르게 넣으면 그룹별 성과를 Report API로 구분 가능.
링크 속성은 `rel="noopener nofollow sponsored"`.

**쿠팡 파트너스 카테고리 ID (2026-09-10 교차검증 확인)**
1001 여성패션 · 1002 남성패션 · 1010 뷰티 · 1011 출산/육아 · 1012 식품 · 1013 주방용품 ·
1014 생활용품 · 1015 홈인테리어 · 1016 가전디지털 · 1017 스포츠/레저 · 1018 자동차용품 ·
1019 도서/음반/DVD · 1020 완구/취미 · 1021 문구/오피스 · 1024 헬스/건강식품 ·
1025 국내여행 · 1026 해외여행 · 1029 반려동물용품 · 1030 유아동패션

30~60대 여성 화이트리스트: **1010, 1012, 1013, 1014, 1015, 1024, 1001** (+1017 일부)

**토스 쉐어링크** — 웹사이트 상시 게시가 약관상 허용되는지 **미확인**. 확인 전까지 보류.
금융앱이라 정부지원 섹션 근처에 두면 대출·투자 권유로 오해될 수 있으므로, 핫딜 섹션 내에서도 맨 아래 배치.

**시안 파일** (`blog-drafts/`, git 제외)
- `쿠팡파트너스_배치3안.html` — 초기 3가지 배치안 비교
- `핫딜섹션_시안.html` — 확정된 안1+안3 혼합안
- `가격비교_시안.html` — 노출/제외 카드와 야간 검사 순서

### (a-1) 🔎 쿠팡 상품 검색 — **2026-09-11 구현**
- 메인 핫딜 칸(안내문 아래)과 `hotdeal.html` 위쪽에 검색창. 메인에서 검색하면 `hotdeal.html?q=검색어` 로 넘어가 결과를 보여줌.
- 검색은 `stats-worker` 의 `GET /search?q=` 가 쿠팡 검색 API를 대신 호출 (키는 브라우저에 절대 안 나감). 혜택존 주소에서 온 요청만 받음.
- 결과: 상품 최대 10개(제휴 링크 카드, 광고 표시) + 「쿠팡에서 ○○ 더 보기」 제휴 링크.
- 같은 검색어는 D1 `search_cache` 에 12시간 저장해 재사용. 쿠팡이 거절하면 10분 쉬고 그동안은 검색 결과 페이지 제휴 링크(딥링크)만 줌.
- 집계에는 **검색어를 남기지 않음** — `search` 종류로 result/cache/empty/link/fail/nokey 개수만.
- **쿠팡 키를 Worker 비밀값으로 넣어야 상품 목록이 나옴**: `bash scripts/set-worker-secrets.sh` (대표 실행, 값은 화면에 안 나옴). 키가 없으면 수수료 없는 일반 쿠팡 검색 링크만 보임.
- 코드: 검색창·스크립트 `scripts/hotdeal.py` (`search_form`, `SEARCH_SCRIPT`), 서버 `stats-worker/src/index.js`. Worker 배포: `cd stats-worker && npx wrangler deploy`.

### (a-1b) 💰 구매·수수료 확인 — **2026-09-11**
- `python3 scripts/sales.py [일수]` — 쿠팡 리포트 API로 클릭·주문·취소·예상 수수료 (최대 30일). 사이트 링크(subId hyetaekzone)는 [혜택존], 그 외는 [기타]로 나눠 표시.
- 쿠팡 집계는 하루쯤 늦음. 사이트 쪽 「구매하러 가기」 누른 횟수는 `scripts/stats.sh` (당일 반영).
- 공식 확인·정산: partners.coupang.com → 리포트.

### (a-1c) 🪙 카카오 로그인 포인트 적립·현금 교환 — **2026-09-12 구현 (카카오 키 넣으면 켜짐)**
**대표 결정 (2026-09-12)**: 포인트 = 그 주문의 쿠팡 수수료 × 10% (설정에서 변경), 1P=1원, **1만 P부터 현금 교환(잔액 전부)**, 적립 완료 후 **1년** 지나면 소멸, 관리자는 대표만, 기프티콘은 나중에, 개인정보 보호책임자 "김개똥 · 010-0000-0000"(임시 — 실제 이름·연락처로 교체 필요: `scripts/reward_pages.py` OFFICER).
- 회원: 카카오 로그인 → 회원마다 쿠팡 링크 이름표(subId `hzm00001` …). 로그인한 브라우저에서 쿠팡 링크를 누르는 순간 `subid=hyetaekzone` → 회원 이름표 (`REWARD_SCRIPT`). 24시간 안의 구매가 쿠팡 리포트에 회원 이름표로 잡힘.
- **매시 5분** Worker 크론 (2026-09-12 대표 요청으로 매일 17시 → 매시간): 쿠팡 주문·취소 리포트(최근 30일) → 회원 주문 저장, 다음 달 25일 된 주문 확정(포인트 고정), 유효기간 지난 포인트 소멸(먼저 쌓인 것부터 쓴 것으로 계산), 지급 5년 지난 계좌 정보 삭제.
- 상태: `적립 예정` → (구매한 달의 다음 달 25일) `적립 완료` / 전액 취소 `적립 취소`. 적립 비율은 주문이 처음 잡힐 때 값으로 고정.
- 적립 비율 변경: 관리자 「설정」(빠른 선택 10·20·30·50%, 10만원 구매 시 적립·1만P까지 필요한 구매액 미리보기) 또는 `bash scripts/set-rate.sh 30` (카카오 전에도 가능). 새로 잡히는 주문부터 적용.
- 주민등록번호(2026-09-12 대표 요청): 현금 교환 때 앞6·뒤7 입력 + 별도 동의, 형식·생년월일 검증, 계좌와 함께 암호화. 관리자 「계좌 보기」에서만 보이고 기록 남음. 설정 「주민등록번호 받기」로 끄기 가능. 세금 신고(원천징수·지급명세서)용 — 실제로 원천징수 대상인지 세무사 확인 필요(대상 아니면 수집 근거가 없어 끄는 게 맞음).
- 현금 교환: 회원이 예금주·은행·계좌번호 입력(동의 필수) → AES-GCM 암호화(Worker 비밀값 `PII_KEY`), 신청 즉시 포인트 차감. 관리자가 직접 이체 후 「지급 완료」, 반려 시 포인트 복구·계좌 정보 즉시 삭제. 처리 중엔 중복 신청·탈퇴 불가.
- 화면: `my.html`(내 포인트·현금으로 받기·적립/사용 내역·적립 규정·로그아웃·탈퇴), `admin.html`(대시보드·현금 교환 처리·회원 목록/상세·정지·메모·포인트 조정·설정·작업 기록, noindex), `privacy.html`. 모두 `scripts/hotdeal.py` → `scripts/reward_pages.py` 로 생성.
- 서버: `stats-worker/src/reward.js`, D1 `hyetaekzone-members` (스키마 `stats-worker/members.sql`). 관리자 API는 관리자 회원 로그인 토큰 또는 Bearer STATS_KEY.
- **켜는 법 (대표)**: developers.kakao.com 앱 → Web 플랫폼 `https://hubrizjeon.github.io` → 카카오 로그인 ON, Redirect URI `https://hyetaekzone-stats.baglebagle.workers.dev/auth/kakao/callback` → 닉네임 필수 동의 → (Client Secret) → `bash scripts/set-kakao-secrets.sh`. 그다음 대표가 my.html 에서 로그인 → `bash scripts/make-admin.sh`(번호 확인) → `bash scripts/make-admin.sh <번호>` → admin.html.
- 키 없이 점검: `node --no-warnings stats-worker/test/kakao-e2e.mjs` — 카카오 로그인 시작·돌아오기(취소·state 위조·토큰 실패), 회원 생성·재로그인, 관리자 지정·권한, 쿠팡 동기화(큰 주문번호·부분 취소·확정), 소멸, 로그아웃 24개 항목. 코드 고친 뒤 배포 전에 돌릴 것 (2026-09-12 전부 통과).
- 점검(2026-09-12): 시험 회원으로 적립·교환 신청·중복 방지·계좌 보기·반려(복구·계좌 삭제)·지급 완료·조정·탈퇴·관리자 권한 전부 통과, 시험 데이터 삭제.
- **2026-09-12 추가 (대표 "니가 만들어야 하는 것 다 만들어줘")**:
  - 포인트 이용약관 `terms.html` (버전 `TERMS_VER` 2026-09-12, reward.js). 첫 로그인 뒤 이용약관·개인정보 필수 동의 전에는 적립 이름표를 붙이지 않고 현금 교환도 막음. 약관 바꾸면 버전을 올려 다시 동의받음.
  - 곧 사라질 포인트: 유효기간 30일 안에 끝나는 미사용 포인트를 내 포인트에 안내(소멸과 같은 계산).
  - **원천징수: 사업소득 3.3% (대표 결정 2026-09-12, 처음 22% 기타소득에서 변경)**: 소득세 3% + 지방소득세 0.3%(소득세의 10%)를 각각 10원 미만 버림으로 계산, 소득세 1,000원 미만(교환 33,333원 이하)은 떼지 않음(소득세법 제86조 소액부징수), 면제 금액 설정은 0. 예: 60,000P → 소득세 1,800 + 지방 180 = 1,980원, 입금 58,020원. cashouts 에 tax_income·tax_local 따로 저장, 지급 내역 CSV 에 소득세·지방소득세·합계 칸. 사업소득 간이지급명세서는 매월 제출 대상일 수 있으니 세무 담당 확인.
  - 지급 내역 엑셀(CSV): 관리자 「현금 교환」의 월 선택 → 예금주·은행·계좌·주민번호·교환P·원천징수세액·실지급액·합계 (지급명세서·원천세 신고용). 받을 때마다 작업 기록에 남음 — 파일은 안전한 곳에 보관.
  - 회원 알림(사이트 안): 내 포인트를 열면 지난번 뒤로 생긴 새 구매·적립 완료·입금·반려·소멸·조정을 맨 위에 보여줌(기기별). 카카오 알림톡은 비즈 채널·비용이 필요해 보류.
  - 목차에 「🪙 내 포인트」 칩(핫딜 옆, hotdeal.py 가 관리).
  - 아침 메일(08:08 예약 작업) 맨 위에 포인트 현황: `python3 scripts/points_report.py` — 교환 대기(있으면 제목에 [교환 대기 N건]), 새 회원, 회원 구매(첫 회원 구매면 🎉), 잔액 합계, 동기화 36시간 실패 경고.
  - 백업: 매주 일요일 03:30 `scripts/backup-members.sh` (launchd `~/Library/LaunchAgents/io.hubriz.hyetaekzone-backup.plist`) → `~/keys/hyetaekzone/backups/` 최근 12개, 로그 backup.log. 이 컴퓨터가 켜져 있어야 함(잠자기면 깨어날 때 실행). D1 Time Travel 로도 되돌릴 수 있음.
  - 블로그 글감 `next` 에 "혜택존|포인트 적립 시작" 추가 — 대표가 적립 비율·보호책임자 확정 후 발행.
  - 점검: `node --no-warnings stats-worker/test/kakao-e2e.mjs` 38개 항목 전부 통과.
- **2026-09-12 21:29 카카오 로그인 개통**: 처음엔 KOE010(클라이언트 시크릿 미등록)으로 실패 → 대표가 시크릿 처리 후 성공. 대표 = 회원 #9903 전창민(이름표 hzm09903), 관리자 지정. 로그인 실패 시 화면에 카카오 오류 번호가 나오고 작업 기록에 '카카오 · 로그인 실패'로 남음.
- 남은 일: 기프티콘, 카카오 알림톡, 이상 감지, 대표 확정(보호책임자 실명·연락처, 적립 비율), 세무사 확인(원천징수 기준·주민번호 수집 근거). 현금 지급의 세무·전자금융 처리는 세무사 확인 권장. 쿠팡 운영정책상 구매 보상 제한 가능성은 대표가 알고 진행.

### (a-2) 홍보·측정 장치 — **2026-09-10 구현**

`scripts/site_kit.py` 가 관리하고, `scripts/hotdeal.py` 가 돌 때마다(23시·08시) 함께 적용·복구합니다.
index.html 의 `HZ-META` · `HZ-TOOLS` · `HZ-SHARE` · `HZ-TRACK` · `HZ-JS` 마커와 `HZ-KIT-CSS` 안쪽은 손으로 고치지 마세요.

| 장치 | 내용 |
|---|---|
| 카톡 공유 미리보기 | og 제목·설명·이미지(`og.png` 1200×630). 핫딜 페이지는 핫딜용 미리보기 |
| 공유하기 (2026-09-12 「동료 선생님께 보내기」에서 이름 변경) | 목차 아래 · 페이지 끝. 휴대폰 공유창(없으면 주소 복사). 공유 주소 `?ref=share` |
| 홈 화면에 추가 | `manifest.webmanifest` + 아이콘(`icon-192/512.png`, `apple-touch-icon.png`). 누르면 기종별 안내(아이폰·삼성·카톡 인앱). 홈 화면에서 열면 `?ref=home` |
| 방문·클릭 집계 | Cloudflare Worker `hyetaekzone-stats` + D1 (`stats-worker/`). **개인정보 없이 날짜별 개수만**. 계정: baglebagle@me.com |

- 조회: `scripts/stats.sh` (최근 7일) · `scripts/stats.sh 30`
- 조회 키: `~/keys/hyetaekzone/stats.txt` (저장소 밖). Worker 비밀값 `STATS_KEY` 와 같아야 함
- 받는 기록: 방문(모바일/PC · share/home 유입), 혜택 버튼, 핫딜 구매 버튼, 목차, 핫딜 카테고리·필터·정렬·더 보기, 보내기, 홈 화면 추가
- 혜택존에서 온 기록만 받음(다른 곳 403). 봇이 부풀릴 수는 있어 절대값보다 추세를 보세요

### (b) 커스텀 도메인 benefits.hubriz.io
DNS 레코드만 넣으면 됨: `CNAME` / `benefits` / `hubrizjeon.github.io.` / TTL 3600
→ DNS 전파 확인 **후에** CNAME 파일 추가 (순서 바꾸면 기존 주소도 일시 접속 불가)

### (c) 성과 측정
GitHub Pages는 서버 로그가 없다. 현재 유입·클릭 추적 수단 없음.

---

## 8. ChatGPT에 물어볼 교차검토 항목

이 문서를 붙여넣고 아래를 요청하면 유용합니다.

1. **정보 신뢰성 구조** — 매일 자동 수집한 혜택 정보의 사실 검증을 어떻게 더 강화할 수 있는가? 자동화된 크로스체크 방법은?
2. **시니어 접근성** — 18px/고대비/큰 버튼 외에 추가로 필요한 것은? WCAG 기준 대비 미흡한 지점은?
3. **제휴 링크 윤리** — 정부 지원 정보와 제휴 광고가 한 페이지에 있을 때, 위 분리 설계로 충분한가? 공정위 심사지침 기준으로 보완할 점은?
4. **자동화 취약점** — 스케줄 작업이 실패해도 조용히 넘어가는 구조. 실패 감지·알림을 어떻게 넣을 것인가?
5. **콘텐츠 낡음 방지** — 날짜만 자동 갱신되고 내용이 낡는 위험을 구조적으로 어떻게 막을 것인가?
6. **확장** — 지역별 혜택(보건소·지자체) 개인화를 정적 사이트에서 어떻게 구현할 수 있는가?

---

## 9. 참고 — 오늘(2026-09-10) 데이터 검증 결과

24개 뱃지 전수 검사, **종료된 항목 0건**.

| 혜택 | 기간 | 상태 |
|---|---|---|
| 마트 4사 명절전단 | 9.10 ~ 9.16 | 🆕 오늘 시작 |
| bhc 구구데이 | 9.7 ~ 9.13 | D-3 |
| 배스킨라빈스 블록팩 | 9.9 ~ 9.13 | D-3 |
| GS25 초특갓세일 | 9.1 ~ 9.15 | D-5 |
| 스타벅스 라떼위크 | 9.7 ~ 9.20 | D-10 |
| K-패스 확대 | ~ 9.30 | D-20 (한시 종료) |
| BBQ 픽업 올데이 | 9월 한 달 | D-20 |
| 문화누리카드 | 발급~11.30 / 사용~12.31 | 장기 |

추석: 2026년 9월 25일(금), 연휴 9/24(목)~9/26(토)
